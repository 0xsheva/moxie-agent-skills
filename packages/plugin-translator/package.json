{
    "name": "@moxie-protocol/plugin-translator",
    "version": "0.0.1",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "dependencies": {
        "@moxie-protocol/core": "workspace:*",
        "openai": "^4.0.0",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@types/node": "^22.10.5",
        "@vitest/coverage-v8": "^2.1.5",
        "tsup": "8.3.5",
        "vitest": "^2.1.5"
    },
    "scripts": {
        "build": "tsup --format esm --dts",
        "dev": "tsup --format esm --dts --watch",
        "lint": "eslint --fix  --cache .",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage"
    }
}
